You are inside an **identity context** - this is also available:

- `IDENTITY trait <name:value|-name ...>` - stage trait value changes/removals
- `OVERRIDE add [value]` - pin the identity to a specific feature variant
- `OVERRIDE delete` - unpin the identity from its pinned variant
