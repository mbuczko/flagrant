apiVersion: v1
kind: Service
metadata:
  name: {{ include "flagrant.fullname" . }}
  labels:
    {{- include "flagrant.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flagrant.selectorLabels" . | nindent 4 }}
